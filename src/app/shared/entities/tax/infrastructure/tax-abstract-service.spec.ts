import { TaxAbstractService } from './tax-abstract-service';

describe('TaxAbstractService', () => {
  it('should create an instance', () => {
    expect(new TaxAbstractService()).toBeTruthy();
  });
});
