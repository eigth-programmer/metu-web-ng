import { ProductAbstractService } from './product-abstract-service';

describe('ProductAbstractService', () => {
  it('should create an instance', () => {
    expect(new ProductAbstractService()).toBeTruthy();
  });
});
