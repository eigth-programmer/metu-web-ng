import { BillingAddressMapper } from './billing-address-mapper';

describe('BillingAddressMapper', () => {
  it('should create an instance', () => {
    expect(new BillingAddressMapper()).toBeTruthy();
  });
});
