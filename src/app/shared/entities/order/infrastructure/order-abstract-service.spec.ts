import { OrderAbstractService } from './order-abstract-service';

describe('OrderAbstractService', () => {
  it('should create an instance', () => {
    expect(new OrderAbstractService()).toBeTruthy();
  });
});
