import { ReviewAbstractService } from './review-abstract-service';

describe('ReviewAbstractService', () => {
  it('should create an instance', () => {
    expect(new ReviewAbstractService()).toBeTruthy();
  });
});
