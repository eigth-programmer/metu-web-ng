import { CategoryAbstractService } from './category-abstract-service';

describe('CategoryAbstractService', () => {
  it('should create an instance', () => {
    expect(new CategoryAbstractService()).toBeTruthy();
  });
});
