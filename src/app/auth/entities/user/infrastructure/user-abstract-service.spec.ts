import { UserAbstractService } from './user-abstract-service';

describe('UserAbstractService', () => {
  it('should create an instance', () => {
    expect(new UserAbstractService()).toBeTruthy();
  });
});
