import { AbstractSessionService } from './abstract-session-service';

describe('AbstractSessionService', () => {
  it('should create an instance', () => {
    expect(new AbstractSessionService()).toBeTruthy();
  });
});
